{
  "name": "notes-board",
  "private": true,
  "version": "1.0.0",
  "description": "MERN notes board — Express API + React client",
  "scripts": {
    "start": "node server/index.js",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mongoose": "^8.9.5"
  }
}
